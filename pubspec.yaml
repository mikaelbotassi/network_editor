name: network_editor
description: "Digital sketch for electrical network design."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  flutter_map_location_marker: ^10.2.0
  tabler_icons_plus: ^3.41.0
  result_dart: ^2.2.0
  flutter_svg: ^2.2.4
  geolocator: ^14.0.2
  simple_painter: ^1.4.0
  path_provider: ^2.1.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
